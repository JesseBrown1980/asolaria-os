name: kernel gate

on:
  pull_request:
  push:
    branches:
      - main
      - liris-kernel-ci-professional-gate-20260708

jobs:
  kernel-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust 1.81 kernel toolchain
        run: |
          rustup toolchain install 1.81.0 --profile minimal --component rustfmt,clippy --target x86_64-unknown-uefi
      - name: Install QEMU and OVMF
        run: |
          sudo apt-get update
          sudo apt-get install -y qemu-system-x86 ovmf
      - name: Run kernel build, unit, artifact, and QEMU smoke gate
        run: |
          cd kernel
          export CARGO_BUILD_JOBS=1
          bash ./scripts/test-suite.sh
